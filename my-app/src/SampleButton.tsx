import * as React from 'react'; // tsrcc import statements at the top of the file. 
// Baseline with tsrcc is going to import baseline react from the reac node_module

export interface IAppProps {
}

export default class App extends React.Component<IAppProps> {
  public render() {
    return (
      <div>
        
      </div>
    );
  }
}
